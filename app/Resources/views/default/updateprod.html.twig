{% extends 'base.html.twig' %}
{% block body %}
<body>
<div class="container">
<h1>Modifier le produit {{produit.name}} ? (#{{produit.id}}) </h1>
    {{ form_start(f) }}
        {{ form_widget(f) }}
    {{ form_end(f) }}
</div>
</body>
{% endblock %}